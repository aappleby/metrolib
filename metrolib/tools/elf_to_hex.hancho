from hancho import *

rules = load("../../rules/rules.hancho")

bin = rules.cpp_bin("elf_to_hex.cpp", "elf_to_hex")

run = Rule(
  command = "{tool} {files_in}",
  files_out = [
    "{swap_ext(files_in, '.code.vh')}",
    "{swap_ext(files_in, '.data.vh')}",
  ],
  tool = bin
)
