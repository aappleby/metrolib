tool = build_config.load("build.hancho")

run = build_config.rule(
  command = "{tool} {rel_source_files} {rel_build_files}",
  build_files = [
    "{swap_ext(source_files, '.code.vh')}",
    "{swap_ext(source_files, '.data.vh')}",
  ],
  build_path = build_config.source_path,
  tool = tool.bin
)
