tools = build_config.load("{repo_path}/metrolib/tools/build.hancho")

run = build_config.rule(
  command = "{command_files} {rel_source_files} {rel_build_files}",
  command_files = tools.elf_to_hex,
)
