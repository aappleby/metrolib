tools = build_config.load("build.hancho")

print(dir(tools))

run = build_config.rule(
  command = "{command_files} {rel_source_files} {rel_build_files}",
  command_files = tools.elf_to_hex,
  build_files = [
    "{swap_ext(source_files, '.code.vh')}",
    "{swap_ext(source_files, '.data.vh')}",
  ],
)
