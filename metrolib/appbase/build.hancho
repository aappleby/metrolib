rules = hancho.load("{repo_path}/rules/rules.hancho")

lib = rules.cpp_lib(
  source_files = hancho.glob("*.cpp"),
  build_files  = "libappbase.a",
  includes = [
    rules.c_config.includes,
    "{repo_path}/symlinks",
  ]
)
